services:
  postgres:
    image: postgres:latest
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: my_password
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql_docker/data
      - ./init/init.sh:/docker-entrypoint-initdb.d/000-init.sh
      - ./init/database_create.sql:/docker-entrypoint-initdb.d/sql/000-database_create.sql

  neo4j:
    image: neo4j:5
    container_name: neo4j
    environment:
      NEO4J_AUTH: neo4j/my_password
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs

  db_fill:
    build:
      context: .
      dockerfile: ./init/Dockerfile.db_fill
    container_name: db_fill
    depends_on:
      - postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: my_password
      POSTGRES_DB: postgres
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
    volumes:
      - ./db_fill:/app/db_fill
      - ./dt_utils:/app/dt_utils
    restart: no

volumes:
  postgres_data:
  neo4j_data:
  neo4j_logs: